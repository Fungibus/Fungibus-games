# Fungibus.github.io

## Development

Use the Node version pinned in `.nvmrc`, then install dependencies:

```sh
nvm use
npm install
```

Run the local Wrangler dev server:

```sh
npm run dev
```

Validate the Cloudflare deploy config without deploying:

```sh
npm run check
```
